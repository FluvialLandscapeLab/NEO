# Check to be sure that necessary attributes exist when running the model.  Boy
# does it crap out with odd messages!
  
# put a loop around execute...

# deal with time step -- divide all fluxes by time step!